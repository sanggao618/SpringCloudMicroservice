server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #配置Nacos地址
        #server-addr: localhost:8848 #单机版
        server-addr: 192.168.160.22:1111 #集群版

management:
  endpoints:
    web:
      exposure:
        include: '*'